node_modules/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
uploads/

# Android build outputs — exclude compiled classes/resources but NOT the final APK
android/app/build/intermediates/
android/app/build/generated/
android/app/build/tmp/
android/.gradle/
android/build/
# frontend and backend dist
frontend/dist/
backend/dist/

# Android release signing — NEVER commit the keystore or its passwords
keystore.properties
*.jks
*.keystore
