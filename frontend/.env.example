# Trading Academy Frontend Environment Configuration
# If deploying to Vercel, it is recommended to use vercel.json for rewrites instead of setting API URL in client code.
# Alternatively, specify this variable if you've configured your code to resolve base URLs.
VITE_API_URL=https://trading-academy-backend.onrender.com
